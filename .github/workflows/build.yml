name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 33
          build-tools: '33.0.2'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-17-jdk autoconf autoconf-archive automake libtool pkg-config libffi-dev zlib1g-dev

      - name: Install Python packages
        run: |
          pip install buildozer cython python-for-android

      - name: Accept SDK licenses
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses 2>/dev/null || true

      - name: Build APK
        run: |
          buildozer android debug
        env:
          BUILDOZER_ANDROID_NDK_API: 21
          BUILDOZER_ANDROID_API: 33
          BUILDOZER_ANDROID_SDK_ROOT: $ANDROID_HOME
        timeout-minutes: 45

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: muyu-debug.apk
          path: bin/*.apk
